# Pinned dependencies for tools/export_mobilesam_onnx.py; consumed by
# .github/workflows/model-bundle.yml (setup-python cache + install step).
torch==2.5.1
torchvision==0.20.1
timm==1.0.29
onnx==1.17.0
onnxruntime==1.20.1
